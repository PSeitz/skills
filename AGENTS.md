# Instructions

If making changes to the code, keep the changes minimal. Added code adds complexity and needs to be justified.

Always describe without fluff.

Do not add content to the LLM context unless explicitly requested. Prefer prompt templates over mechanisms such as skills that add content to the context automatically.
