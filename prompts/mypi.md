---
name: mypi
description: Make changes to the PSeitz/skills Pi configuration repository
---

Work in `~/.pi/agent/git/github.com/PSeitz/skills` and implement: $@

Before editing:

1. Confirm the directory is the `PSeitz/skills` Git repository.
2. Inspect its current branch, status, and applicable instructions.
3. Preserve unrelated local changes. Do not reset or clean them.
4. If `$@` is empty, ask what should be changed.

Make the smallest suitable change and validate it. Commit the completed change with a concise message and push it to the configured remote unless explicitly asked not to. Summarize the changed files, validation, commit, and push result.
